syntax = "proto3";

package app;

message Msg {

    message Detect {
        int32 type = 1;  // 0 - facecar, 1 - cigarette
        bytes image = 2;
    }

    oneof msg {
        Detect detect = 1;
    }
}