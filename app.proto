syntax = "proto3";

package app;

message Task {
    optional uint64 id = 1;
}

message Detect {
    int32 type = 1;  // 0 - facecar, 1 - cigarette
    bytes image = 2;
}

message Msg {
    oneof msg {
        Task task = 1;
        Detect detect = 2;
    }
}